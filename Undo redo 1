class TextEditor:
    def __init__(self):
        self.text = ""
        self.undo = []
        self.redo = []

    def make_change(self, new_text):
        self.undo.append(self.text)
        self.redo.clear()
        self.text = new_text
        print("Change made:", self.text)

    def undo_change(self):
        if self.undo:
            self.redo.append(self.text)
            self.text = self.undo.pop()
            print("Undo:", self.text)
        else:
            print("Nothing to undo.")

    def redo_change(self):
        if self.redo:
            self.undo.append(self.text)
            self.text = self.redo.pop()
            print("Redo:", self.text)
        else:
            print("Nothing to redo.")


# Example use
editor = TextEditor()
editor.make_change("Hello")
editor.make_change("Hello, how are you?")
editor.undo_change()
editor.redo_change()
